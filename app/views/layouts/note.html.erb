<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title><%= @page_title %></title>
    <%= csrf_meta_tags %>

    <meta name="keywords" content="<%= @seo_keywords %>" />

    <%= stylesheet_link_tag    'notes', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <%= render 'shared/note_nav' %>

    <!-- start of note header -->
    <div class="note-header">
      <div class="container">
        <h1 class="note-title">Notes</h1>
        <p class="lead note-description">An example Note template built with Bootstrap.</p>
        <%# link_to 'Write a New Note', new_note_path if logged_in?(:site_admin) %>
        <%= link_to 'Write a New Note', new_note_path %>
      </div>
    </div>
    <!-- end of note header -->

    <div class="container">

      <div class="row">

        <%= yield %>

        <%= render 'shared/note_sidebar' %>
      </div>

        <!-- start of footer -->
        <footer class="note-footer">
          <p>Note template built for <a href="https://getbootstrap.com">Bootstrap</a> by <a href="https://twitter.com/mdo">@mdo</a>.</p>
          <p>
            <a href="#">Back to top</a>
          </p>
        </footer>
        <!-- end of footer -->
    </div>
  </body>
</html>